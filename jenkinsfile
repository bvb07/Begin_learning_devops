pipeline {
    agent any
    stages {
        stage('start'){
            steps{
                sh '''
                cd compose
                pwd
                ls -a
                docker --version
                '''
                echo '*************************************************'
            }
        // stage('build'){
        //     steps{
        //         sh '''
        //         docker-compose up -d --build
        //         curl -v http:localhost:3000
        //         '''
        //         echo '*************************************************'
        //     }
        // }
        // stage('stop'){
        //     steps{
        //         sh '''
        //         docker-compose down --volumes
        //         '''
        //     }
        // }
       
        }

        
    }
}