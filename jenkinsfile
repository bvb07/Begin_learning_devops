pipeline {
    agent any
    stages {
        stage('start'){
            steps{
                sh '''
                cd compose
                pwd
                docker --version
                ls -a
                '''
                echo '*************************************************'
            }
          }
        stage('build'){
            steps{
                sh '''
                cd compose
                docker-compose up -d
             

                '''
                echo '*************************************************'
              }
          }
        stage('stop'){
            steps{
                sh '''
                cd compose
                docker-compose down 
                kubectl config view 
                kubectl config set-credentials
                '''
           }
        }
       


        
    }
}
