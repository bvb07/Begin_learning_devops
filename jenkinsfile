pipeline {
    agent any
    stages {
        stage('start'){
            steps{
                sh '''
                cd compose
                docker --version
                docker info
                crul --version
                docker-compose version
                '''
            }
        }
        // stage('Check') {
        //     steps {
        //         sh 'ls -a'
        //     }
        // }
        // stage('Stop') {
        //     steps {
        //         sh 'docker-compose down --volumes'
        //     }
        // }

        
    }
}