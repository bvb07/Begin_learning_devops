pipeline {
    agent any
    stages {
        stage('start'){
            steps{
                sh '''
                cd compose
                pwd
                ls -a
                '''
            }
        }
        // stage('Check') {
        //     steps {
        //         sh 'ls -a'
        //     }
        // }
        // stage('Stop') {
        //     steps {
        //         sh 'docker-compose down --volumes'
        //     }
        // }

        
    }
}