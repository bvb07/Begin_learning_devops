pipeline {
    agent any
    stages {
        stage('start'){
            steps{
                sh 'cd compose'
            }
        }
        stage('Build') {
            steps {
                sh 'docker-compose build'
            }
        }
        stage('Deploy') {
            steps {
                sh 'docker-compose up -d'
            }
        }
    }
}